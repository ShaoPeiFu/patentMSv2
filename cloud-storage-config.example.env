# 云端存储配置示例
# 复制此文件为 .env 并根据您的云端存储提供商进行配置

# 云端存储提供商 (aws, azure, google, aliyun)
CLOUD_PROVIDER=aws

# 云端存储桶名称
CLOUD_BUCKET=patent-backups

# 云端存储区域
CLOUD_REGION=us-east-1

# 云端存储访问密钥
CLOUD_ACCESS_KEY=your_access_key_here

# 云端存储密钥
CLOUD_SECRET_KEY=your_secret_key_here

# 本地备份目录（备用）
BACKUP_DIR=./backups

# 示例配置说明：

# AWS S3 配置示例：
# CLOUD_PROVIDER=aws
# CLOUD_BUCKET=my-patent-backups
# CLOUD_REGION=us-west-2
# CLOUD_ACCESS_KEY=AKIAIOSFODNN7EXAMPLE
# CLOUD_SECRET_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# Azure Blob Storage 配置示例：
# CLOUD_PROVIDER=azure
# CLOUD_BUCKET=patentbackups
# CLOUD_REGION=eastus
# CLOUD_ACCESS_KEY=your_storage_account_name
# CLOUD_SECRET_KEY=your_storage_account_key

# Google Cloud Storage 配置示例：
# CLOUD_PROVIDER=google
# CLOUD_BUCKET=patent-backups-bucket
# CLOUD_REGION=us-central1
# CLOUD_ACCESS_KEY=your_service_account_key
# CLOUD_SECRET_KEY=your_service_account_secret

# 阿里云OSS 配置示例：
# CLOUD_PROVIDER=aliyun
# CLOUD_BUCKET=patent-backups
# CLOUD_REGION=oss-cn-hangzhou
# CLOUD_ACCESS_KEY=your_access_key_id
# CLOUD_SECRET_KEY=your_access_key_secret

# 注意事项：
# 1. 请确保云端存储桶已创建并配置了适当的权限
# 2. 访问密钥应具有读写权限
# 3. 建议使用IAM角色或服务账户而不是硬编码的密钥
# 4. 定期轮换访问密钥以提高安全性
